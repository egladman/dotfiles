#!/usr/bin/env sh

# Wrapper for picom that supports appending flags

if [ -f "${HOME}/.config/picom-flags.conf" ]; then
  while IFS= read -r line || [ -n "$line" ]; do
    set -- "$@" $line
  done < "${HOME}/.config/picom-flags.conf"
fi

/usr/bin/picom "$@"
